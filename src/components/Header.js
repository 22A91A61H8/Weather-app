import React from 'react';
import '../css/header.css'; // ✅ Correct path to the CSS file

const Header = () => {
  return (
    <header className="header">
      Weather App
    </header>
  );
};

export default Header;
